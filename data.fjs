// $
function eval() {
  return null;
}

process.env = {}

const server = {
  name: '$.guild.name',
  id: '$.guild.id',
}

const channel = {
  send({ content }) {
    if (content) return true;
    return false;
  }
}