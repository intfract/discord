// $
function eval() {
  return null;
}

process.env = {}

const server = {
  name: '$.guild.name',
  id: '$.guild.id',
}

client.token = null

const channel = {
  send({ content, embeds }) {
    if (content) return content;
    return new Error('Message has no content!');
  }
}